# tcc_good_code/envs.yaml

app:
  name: "tcc_good_code"
  version: "1.0.0"
  port: 5000

database:
  url: "sqlite:///./data/database.db"

logging:
  level: "WARNING"
  log_file: "logs/app.log"

smtp:
  server: "smtp.office365.com"
  port: 587
  user: "bot_assinaturas@outlook.com"
  password: "senha_secreta"

